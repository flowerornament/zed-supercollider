---
description: Show git and bd status together
---

Show current state of repo and tasks.

## Run

1. `git status --short` - uncommitted changes
2. `git log --oneline -3` - recent commits
3. `bd ready` - tasks ready to work on

Summarize: branch, uncommitted changes count, ready task count.
