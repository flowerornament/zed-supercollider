---
description: Sync bd issues to git
---

Run `bd sync` to export task changes and commit .beads/ updates.

This runs automatically as part of `/commit`, but use `/sync` standalone when:
- You've updated tasks and want to checkpoint without other changes
- You want to pull remote task changes
